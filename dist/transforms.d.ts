/**
 * Builds the combined CSS transform string based on extracted CSS variables
 */
export declare const buildTransform: (el: HTMLElement, normalizedPercentage: number, scrollY: number, mouseX: number, mouseY: number, speed: number) => string;
